# Code.gs pointer — V6.6.3 OPTIMIZED BULK

Sumber kebenaran backend: **Apps Script di spreadsheet** (bukan file ini).

Versi target setelah paste:
- `BACKEND_VERSION = "6.6.3+OPT-BULK+HOLD-SHORT+DASH-PC"`
- `APP_TITLE = "BACKEND GudangAI-69 V6.6.3 OPTIMIZED BULK"`

File lengkap disiapkan di sesi agent:
`Code_GudangAI_V6.6.3_OPTIMIZED_BULK.gs`

Fitur utama:
- bulkTransaction: 1 lock, 1x load stok, setValues bulk
- HOLD keterangan: `order N, sisa X kurang Y`
- Dashboard PC-v2: divisi bar, volume, donat PO, tabel PO minggu ini/lalu
- setupEnvironment → refreshDashboard(true) otomatis

Setelah paste: Save → setupConfigSheet → setupEnvironment → Deploy New version.
